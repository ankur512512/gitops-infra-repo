apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: gitops-repo
  namespace: gitops-control
spec:
  freightCreationPolicy: Automatic
  interval: 5m0s
  subscriptions:
  - git:
      repoURL: https://github.com/ankur512512/gitops-infra-repo.git
      branch: main
      alias: gitops
      includePaths:
      - apps/gitops-app/base
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: dev
  namespace: gitops-control
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: gitops-repo
    sources:
      direct: true
  promotionTemplate:
    spec:
      steps:
      - uses: git-clone
        config:
          repoURL: https://github.com/ankur512512/gitops-infra-repo.git
          checkout:
          - branch: main
            path: out
      - uses: kustomize-set-image
        config:
          path: out/apps/gitops-app/overlays/dev
          images:
          - image: nginx
            tag: {{ "${{ commits.gitops.id }}" }}
      - uses: git-commit
        config:
          path: out
          message: {{ "Promoted to dev: ${{ commits.gitops.id }}" | quote }}
      - uses: git-push
        config:
          path: out
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: stage
  namespace: gitops-control
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: gitops-repo
    sources:
      stages:
      - dev
  promotionTemplate:
    spec:
      steps:
      - uses: git-clone
        config:
          repoURL: https://github.com/ankur512512/gitops-infra-repo.git
          checkout:
          - branch: main
            path: out
      - uses: kustomize-set-image
        config:
          path: out/apps/gitops-app/overlays/stage
          images:
          - image: nginx
            tag: {{ "${{ commits.gitops.id }}" }}
      - uses: git-commit
        config:
          path: out
          message: {{ "Promoted to stage: ${{ commits.gitops.id }}" | quote }}
      - uses: git-push
        config:
          path: out
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod
  namespace: gitops-control
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: gitops-repo
    sources:
      stages:
      - stage
  promotionTemplate:
    spec:
      steps:
      - uses: git-clone
        config:
          repoURL: https://github.com/ankur512512/gitops-infra-repo.git
          checkout:
          - branch: main
            path: out
      - uses: kustomize-set-image
        config:
          path: out/apps/gitops-app/overlays/prod
          images:
          - image: nginx
            tag: {{ "${{ commits.gitops.id }}" }}
      - uses: git-commit
        config:
          path: out
          message: {{ "Promoted to prod: ${{ commits.gitops.id }}" | quote }}
      - uses: git-push
        config:
          path: out
